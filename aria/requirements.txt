# Aria — Python Dependencies
# ===========================
# Install: pip install -r aria/requirements.txt
#
# Grouped by component for clarity.

# ── Core Framework ─────────────────────────────────────────────────────────────
fastapi==0.115.0
uvicorn[standard]==0.30.0
pydantic==2.9.0
python-multipart==0.0.12
websockets==13.1

# ── Memory Layer ──────────────────────────────────────────────────────────────
# Redis session manager
redis==5.1.0                  # Local Redis client
upstash-redis==1.3.0          # Upstash cloud Redis (free tier)

# Postgres / SQLite storage
sqlalchemy==2.0.35
psycopg2-binary==2.9.9        # PostgreSQL driver (Supabase)
# sqlite3 is built-in (Python std library)

# ── AI & LangChain ────────────────────────────────────────────────────────────
langchain==0.3.0
langchain-openai==0.2.0
langchain-groq==0.2.0
langgraph==0.2.28             # Multi-agent orchestration
openai==1.50.0

# ── Integrations ──────────────────────────────────────────────────────────────
# Google Calendar
google-auth==2.35.0
google-auth-oauthlib==1.2.1
google-api-python-client==2.148.0

# Twilio (WhatsApp + SMS)
twilio==9.2.3

# ── Utilities ─────────────────────────────────────────────────────────────────
python-dotenv==1.0.1          # .env loading
httpx==0.27.0                 # Async HTTP client
aiofiles==24.1.0              # Async file I/O
requests==2.31.0              # Sync HTTP client (Bolna client)
apscheduler==3.10.4           # Background task scheduling


# ── Testing ───────────────────────────────────────────────────────────────────
pytest==8.3.0
pytest-asyncio==0.24.0
pytest-cov==5.0.0
httpx==0.27.0                 # FastAPI TestClient
